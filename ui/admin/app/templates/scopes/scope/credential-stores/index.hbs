<Rose::Layout::Page as |page|>
  <page.header>
    <h2>
      {{t 'resources.credential-stores.title_plural'}}
    </h2>
    <p>{{t 'resources.credential-stores.description'}}</p>
  </page.header>

  <page.breadcrumbs>Breadcrumbs</page.breadcrumbs>
  <page.actions>Actions</page.actions>

  <page.body>
    {{#if @model}}
      <Rose::Table as |table|>
        <table.header as |header|>
          <header.row as |row|>
            <row.headerCell>{{t 'form.name.label'}}</row.headerCell>
            <row.headerCell>{{t 'form.type.label'}}</row.headerCell>
            <row.headerCell>{{t 'form.id.label'}}</row.headerCell>
          </header.row>
        </table.header>

        <table.body as |body|>
          {{#each @model as |credentialStore|}}
            <body.row as |row|>
              <row.headerCell>
                {{credentialStore.displayName}}
                <p>{{credentialStore.description}}</p>
              </row.headerCell>
              <row.cell>{{credentialStore.type}}</row.cell>
              <row.cell>{{credentialStore.id}}</row.cell>
            </body.row>
          {{/each}}
        </table.body>
      </Rose::Table>
    {{/if}}
  </page.body>
</Rose::Layout::Page>